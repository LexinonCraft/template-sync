version: snapshot

templates:
  assignment:
    description: Test template
    parameters:
      - name: name
    files:
      - source: templates/base.typ
        target: base.typ
        mode: static
        jinja: false
      - source: templates/doc.typ.j2
        target: main.typ
        mode: dynamic
        jinja: true
