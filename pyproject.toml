[project]
name = "template-sync"
version = "0.1.0"
description = ""
authors = [
    {name = "LexinonCraft",email = "willi4wuff@gmail.com"}
]
requires-python = ">=3.14"

[tool.poetry.dependencies]
click = "^8.4.2"
questionary = "^2.1.1"
jinja2 = "^3.1.6"
more-termcolor = "^1.1.3"
gitpython = "^3.1.61"
pyyaml = "^6.0.3"

[tool.poetry.group.dev.dependencies]
pytest = "^9.1.1"
ruff = "^0.16.8"

[tool.poetry.scripts]
template-sync = "template_sync.cli:entrypoint"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
testpaths = ["tests"]
